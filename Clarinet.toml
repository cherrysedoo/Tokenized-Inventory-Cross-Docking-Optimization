[project]
name = 'Tokenized-Inventory-Cross-Docking-Optimization'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.cross-dock-manager]
path = 'contracts/cross-dock-manager.clar'
clarity_version = 2
epoch = 2.5

[contracts.scheduling-coordination]
path = 'contracts/scheduling-coordination.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
